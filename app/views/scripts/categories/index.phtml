

<h2 class="text-3xl font-bold text-brandBrown mb-6">Categories List</h2>

<div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-bold text-brandBrown mb-6">Listado de categorías</h2>
    <a href="<?= WEB_ROOT ?>/categories/create"
       class="bg-brandBrown text-white px-4 py-2 rounded-lg shadow hover:bg-brandBrown/90 transition">
        + Create category
    </a>
</div>

<div class="overflow-x-auto bg-white shadow rounded-lg">
    <table class="w-full text-left border-collapse">
        <thead class="bg-brandBrown text-white">
            <tr>
                <th class="p-3">ID</th>
                <th class="p-3">Name</th>
                <th class="p-3">Description</th>
                <th class="p-3 text-center">Actions</th>
            </tr>
        </thead>

        <tbody>
            <?php foreach ($this->categories as $c): ?>
                <tr class="border-b hover:bg-brandBeige/30 transition">
                    
                    <td class="p-3"><?= $c->id ?></td>
                    <td class="p-3"><?= htmlspecialchars($c->category_name) ?></td>
                    <td class="p-3"><?= htmlspecialchars($c->category_description) ?></td>

                    <td class="p-3 text-center space-x-3">


                        <a href="<?= WEB_ROOT ?>/categories/edit/<?= $c->id ?>"
                           class="text-green-700 font-semibold hover:underline">
                            ✏️ Edit
                        </a>


                        <form action="<?= WEB_ROOT ?>/categories/delete/<?= $c->id ?>" 
                              method="POST" 
                              class="inline"
                              onsubmit="return confirm('Are you sure you want to delete this category?');">

                            <button type="submit"
                                    class="text-red-600 font-semibold hover:underline">
                                🗑️ Delete
                            </button>

                        </form>

                    </td>

                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>
