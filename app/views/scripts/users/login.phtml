
<html lang="en">
<div class="max-w-md mx-auto bg-white rounded-x1 shadow p-8">

    <h2 class="text-3x1 font-semibold text-brandBrown text-center mb-6">
        Login Form
    </h2>

    <?php
    $user = $this->user;
    if(!empty($error)):?>
        <p class="text-red-600 text-center mb-4"><?= $error ?></p>
    <?php endif; ?>

    <form action="<?= WEB_ROOT ?>/users/login" method="POST" class="space-y-4">

        <div>
            <label class="block text-brandBrown font-medium">User name: </label>
            <input type = "text" id = "user_username" name = "user_username"
                class="w-full border border-brandBrown/30 rounded px-3 py-2 focus:outline-none focus:ring focus:ring-brandBrown/40"
                required>
        </div>

        <div>
            <label class="block text-brandBrown font-medium">Password: </label>
            <input type = "password" id = "user_password" name = "user_password"
                class="w-full border border-brandBrown/30 rounded px-3 py-2 focus:outline-none focus:ring focus:ring-brandBrown/40"
                required>
        </div>

        <button type = "submit"
                class="w-full bg-brandBrown text-white py-2 rounded-1g shadow hover:bg-brandBrown/90 transition">
            Login
        </button>
    </form>

    <p class="text-center mt-4 text-brandBrown/80">
        You don't have an account?
        <a href="<?= WEB_ROOT ?>/users/register" class="text-brandBrown font-semibold underline">Register here</a>
    </p>

</div>